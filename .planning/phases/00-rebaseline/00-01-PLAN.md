# Phase 00 Plan 00-01 — Establish Truthful Baseline

## Objective

Create the new planning authority without changing product/runtime behavior.

## Tasks

1. Create `.planning/PROJECT.md` with mission, locked decisions, reality audit, active requirements and frozen work.
2. Create `.planning/ROADMAP.md` with dependency-ordered milestones/phases.
3. Create `.planning/STATE.md` with current position, decisions and backlog.
4. Create Phase 00 Context/Plan and Phase 01 Context/Plan.
5. Update `AGENTS.md` so future code work must read `.planning/STATE.md`, PROJECT, current CONTEXT and PLAN first.
6. Reconcile `docs/STATUS.md` wording so Skill and MCP are not presented as fully completed runtime capabilities.
7. Verify Git diff contains planning/docs/rules only; run `git diff --check`.
8. Commit the planning reset.
9. Mark Phase 00 complete in STATE and set current position to Phase 01 / Plan 01-01. Do not implement Phase 01 feature code in the Phase 00 commit.

## Verification

- `git diff --check`
- `git status --short`
- manual review that no `.go`, frontend, build script or generated file changed

## Non-goals

- Skill implementation
- MCP implementation
- verifier implementation
- Desktop changes
- build/package changes