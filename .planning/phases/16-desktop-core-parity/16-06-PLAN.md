# 16-06 — ime-lock-v2 reference alignment and release gate

User request 2026-09-10: compare Desktop tray/autostart/icon lifecycle against https://github.com/wanstu/ime-lock-v2 and decide whether the post-RC3 tree is releaseable.

Reference evidence: temporary ignored clone `.tmp/ime-lock-v2-reference` at upstream commit `43f9a19d0256d47acd561244b866f44d360633ed` (`v0.1.4-rc1`). Relevant files: `tray_manager.go`, `autostart_windows.go`, `single_instance_windows.go`, `main.go`, `app.go`, `wails.json`, `scripts/generate-app-icon.ps1`, `.github/workflows/ci.yml`.

Scope:
- align Windows tray implementation with the reference's `github.com/gogpu/systray` object/message-loop model and start it only after Wails DOM readiness;
- use the canonical `assets/icons/ai-dev-manager-tray.png` bytes directly from Go instead of a generated embedded ICO;
- move Desktop icon preparation into a Wails `frontend:build` hook so direct `wails build` and `scripts/build-desktop.ps1` share the same deterministic icon path;
- retain Wails v2 `SingleInstanceLock` rather than importing the reference's lock-file/wake-file mechanism because the official lock is already implemented and real-exe smoke verified;
- retain `ai-dev-manager-app.png` as ADM's executable icon and `ai-dev-manager-window.png` as UI brand artwork; unlike ime-lock-v2, ADM's window artwork is a detailed UI illustration and is not suitable as the small Windows app icon.

Gates: focused Desktop tests, `go test -count=1 ./...`, `go vet ./...`, `git diff --check`, exact Wails build, hidden startup + second-instance smoke. Human tray-menu/registry/login/DPI acceptance and remote GitHub Actions remain explicit release gates. Local commits only; no tag/push/release without explicit authorization.
