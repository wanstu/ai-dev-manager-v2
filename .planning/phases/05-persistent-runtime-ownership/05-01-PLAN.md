---
phase: 05-persistent-runtime-ownership
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/mcp_health.go
  - internal/app/mcp_health_test.go
  - internal/gateway/runtime_owner.go
  - internal/gateway/runtime_owner_test.go
  - internal/gateway/server.go
  - internal/gateway/lifecycle.go
  - internal/gateway/lifecycle_test.go
  - internal/gateway/mcp_acceptance_test.go
  - cmd/ai-dev-manager/main.go
  - cmd/ai-dev-manager/main_test.go
  - .planning/phases/05-persistent-runtime-ownership/
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03, ADM-GOAL-001, ADM-CORE-003, ADM-CORE-004, ADM-CORE-012, ADM-GW-003, ADM-GW-004, ADM-DEV-003]
must_haves:
  truths:
    - A running Gateway is the one observable owner of long-lived external MCP sessions.
    - Persisted desired state and in-memory observed/session state remain separate.
    - Restart reconstructs sessions from desired state and cannot reuse stale healthy observation.
    - Disable/removal/shutdown closes owned sessions.
    - File/non-Git development remains independent of MCP ownership.
  artifacts:
    - path: internal/gateway/runtime_owner.go
      provides: process-instance owner identity, session cache, reconciliation and cleanup
    - path: .planning/phases/05-persistent-runtime-ownership/05-VERIFICATION.md
      provides: observed restart and cleanup evidence
  key_links:
    - from: Environment.EnabledMCPIDs + MCP catalog
      to: runtimeOwner
      via: startup/operation reconciliation
    - from: runtimeOwner
      to: environment_mcp_status/tools/call
      via: owned ClientSession reuse and active health verification
---

<objective>
Implement the first persistent runtime ownership slice by making the existing long-lived Gateway own external MCP client sessions, expose one process-instance owner identity, rebuild live state from persisted Environment/catalog desired state after restart, and clean resources deterministically. Do not implement Phase 6 dev-process management.
</objective>

<context>
@AGENTS.md
@docs/PRODUCT_CONTRACT.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-persistent-runtime-ownership/05-CONTEXT.md
@.planning/phases/05-persistent-runtime-ownership/05-RESEARCH.md
@.planning/phases/05-persistent-runtime-ownership/05-VALIDATION.md
@.planning/phases/03-external-mcp-runtime-completion/03-CONTEXT.md
</context>

<tasks>
<task type="auto">
<name>1. Establish canonical activation resolution and owner lifecycle</name>
<files>internal/app/mcp_health.go; internal/app/mcp_health_test.go; internal/gateway/runtime_owner.go; internal/gateway/runtime_owner_test.go</files>
<action>Extract the existing Environment selection/catalog/transport/env-reference checks into one app-owned activation-resolution path used by both transient health probing and the Gateway owner. Add an in-memory runtimeOwner with process-instance ID/PID/start time, desired-state reconciliation, owned MCP session map, structured observed status, stale-session eviction and idempotent Close. Persist no session/health/owner fields. Use a small session interface/connect function seam for deterministic lifecycle tests.</action>
<verify>Unit tests prove disabled/configured/error semantics are unchanged; owner reuses one session, drops dead sessions, rebuilds using the same persisted desired state in a fresh owner, and closes on disable/removal/shutdown.</verify>
<done>One reusable in-process owner exists without a second daemon or new product prerequisite.</done>
</task>

<task type="auto">
<name>2. Route Gateway MCP runtime through the owner and expose identity</name>
<files>internal/gateway/server.go; internal/gateway/lifecycle.go; internal/gateway/lifecycle_test.go; cmd/ai-dev-manager/main.go; cmd/ai-dev-manager/main_test.go</files>
<action>Create one runtimeOwner for each real HTTP/stdio Gateway lifetime. Start bounded reconciliation from persisted desired state; `environment_mcp_status/tools/call` use the owner when present and retain local structured failure semantics. Environment MCP disable/remove closes corresponding sessions. Gateway cancellation defers owner Close. Extend health/gateway_info and CLI gateway status with the same owner ID while keeping older health responses readable enough for safe lifecycle handling.</action>
<verify>Two independent requests to one Gateway report the same owner ID; restart reports a different owner ID. Existing Gateway start/status/stop and incompatible-endpoint tests remain green.</verify>
<done>Independent clients observe one persistent resource owner and external MCP calls no longer connect-per-operation in the real Gateway path.</done>
</task>

<task type="auto">
<name>3. Real restart reconciliation and regression gate</name>
<files>internal/gateway/mcp_acceptance_test.go; .planning/phases/05-persistent-runtime-ownership/05-01-SUMMARY.md; 05-VERIFICATION.md; 05-UAT.md; .planning/STATE.md; .planning/PROJECT.md; .planning/ROADMAP.md</files>
<action>Extend real Streamable HTTP acceptance to prove session reuse across separate clients, desired selection persistence across a task-owned Gateway restart, fresh owner/session identity after restart, stale healthy state not surviving a dead upstream, and deterministic cleanup. Run a task-owned current-source Gateway with private ADM_V2_HOME and loopback port for observed acceptance without touching shared 41137. Run deterministic planning checks only; no LLM verifier. Update planning truth only after evidence passes.</action>
<verify>L1-L8 plus `go test ./...`, `go vet ./...`, `git diff --check`; real private Gateway restart/cleanup evidence retained under Phase 5 evidence.</verify>
<done>Phase 5 is locally verified and stops for the next normal integration/review decision; Phase 6 is not started automatically.</done>
</task>
</tasks>

<success_criteria>
L1-L8 pass. One persistent Gateway owns live MCP sessions; desired and observed state are distinct; restart reconciles rather than deserializes; cleanup is deterministic; no Phase 6 process/log/port API, Desktop feature, compatibility layer, or LLM subagent is introduced.
</success_criteria>
