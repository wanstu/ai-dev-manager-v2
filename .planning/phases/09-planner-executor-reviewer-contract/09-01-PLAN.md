---
phase: 09-planner-executor-reviewer-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PRODUCT_CONTRACT.md
  - internal/gateway/runtime_run.go
  - internal/gateway/runtime_workflow.go
  - internal/gateway/runtime_workflow_test.go
  - internal/gateway/workflow_acceptance_test.go
  - internal/gateway/server.go
  - .planning/phases/09-planner-executor-reviewer-contract/
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FLOW-01, ARUN-01, VERIFY-03, ADM-CORE-005, ADM-CORE-007, ADM-GW-001, ADM-GW-003, ADM-DEV-001, ADM-DEV-003]
must_haves:
  truths:
    - Planner/Executor/Reviewer responsibilities are represented as explicit structured audit data, not hidden prompt conventions.
    - Workflow execution reuses the existing persistent `run_` lifecycle and Runtime/verifier authority.
    - A normal verifier failure is visibly a review rejection and not an orchestration/runtime error.
    - Executor and reviewer infrastructure failures remain distinct and auditable in Run status.
    - Plan, per-step evidence, review results and workflow outcome remain visible to later clients through `run_status`.
  artifacts:
    - path: internal/gateway/runtime_workflow.go
      provides: deterministic planning, sequential execution and structured reviewer semantics on Agent Runs
    - path: internal/gateway/server.go
      provides: `run_workflow_start` Agent Gateway entrypoint
    - path: .planning/phases/09-planner-executor-reviewer-contract/09-VERIFICATION.md
      provides: real HTTP and negative classification evidence
  key_links:
    - from: run_workflow_start
      to: runtimeOwner.runs
      via: workflow-kind stable `run_` identity
    - from: workflow executor
      to: runtime.Runtime.Exec
      via: prevalidated allowlisted step commands and owner-derived Run context
    - from: workflow reviewer
      to: app.Service.RunVerifier
      via: existing Environment verifier definitions and writer authority
---

<objective>
Implement FLOW-01 as one deterministic Planner/Executor/Reviewer workflow on the existing Gateway-owned Agent Run lifecycle, with review rejection explicitly distinct from orchestration/runtime failure.
</objective>

<context>
@AGENTS.md
@docs/PRODUCT_CONTRACT.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-planner-executor-reviewer-contract/09-CONTEXT.md
@.planning/phases/09-planner-executor-reviewer-contract/09-RESEARCH.md
@.planning/phases/09-planner-executor-reviewer-contract/09-VALIDATION.md
@internal/gateway/runtime_run.go
@internal/app/service.go
@internal/verifier/service.go
</context>

<tasks>
<task type="auto">
<name>1. Extend Agent Run with structured workflow plan/executor/reviewer state</name>
<files>docs/PRODUCT_CONTRACT.md; internal/gateway/runtime_run.go; internal/gateway/runtime_workflow.go; internal/gateway/runtime_workflow_test.go</files>
<action>Define FLOW-01 product semantics and extend `agentRunStatus` with Run kind plus optional workflow audit. Materialize an immutable explicit plan with stable ordered step IDs before Run installation. Validate every executor command using the existing Runtime authority, then execute sequentially on the owner-derived Run context while recording per-step evidence. After successful execution, call existing `app.Service.RunVerifier` for requested verifier IDs and record structured results. A normal failed verifier produces review/outcome `rejected` while Run orchestration reaches `succeeded`; executor failures and reviewer invocation errors produce Run `failed` with distinct error kinds. Reuse existing Run heartbeat/cancel/owner cleanup and no-persistence semantics.</action>
<verify>P1-P8 focused tests cover plan visibility, authority preflight, accepted/rejected review, executor/reviewer error classification and cancellation.</verify>
<done>Planner/Executor/Reviewer are real typed runtime contracts on one `run_` resource, not labels or hidden shell behavior.</done>
</task>

<task type="auto">
<name>2. Expose deterministic workflow start through the Agent Gateway and prove real HTTP consumption</name>
<files>internal/gateway/server.go; internal/gateway/workflow_acceptance_test.go</files>
<action>Add `run_workflow_start` with explicit goal, ordered command steps, verifier IDs and output bound. Keep list/status/cancel on the existing Run tools. Through real Streamable HTTP, start a real helper workflow, disconnect the launching client, inspect the same Run from a later client, and prove plan/step/review audit for accepted and rejected reviewer outcomes. Verify unrelated file/read and command-Run behavior remains available.</action>
<verify>P9 plus existing Phase 8 HTTP/restart regression.</verify>
<done>External Agents can run and audit the first deterministic Planner/Executor/Reviewer flow without a second orchestration API or lifecycle.</done>
</task>

<task type="auto">
<name>3. Full regression and Phase 9 local verification evidence</name>
<files>.planning/phases/09-planner-executor-reviewer-contract/09-01-SUMMARY.md; 09-VERIFICATION.md; 09-UAT.md; evidence/regression.json; .planning/STATE.md; .planning/PROJECT.md; .planning/ROADMAP.md</files>
<action>Run focused/race/real-HTTP acceptance, full Gateway and repository regression, vet and diff-check. Record evidence and move FLOW-01 to locally verified only when accepted/rejected/error classification is machine-proven. Audit the diff for absence of Phase 10 GSD state advancement, Phase 11 parallel orchestration and automatic Git integration. Do not merge master, push, start Phase 10, or invoke LLM subagents.</action>
<verify>`go test ./...`; `go vet ./...`; `git diff --check`; repeated workflow HTTP acceptance.</verify>
<done>Phase 9 stops locally verified for integration review with Phase 10 not started.</done>
</task>
</tasks>

<success_criteria>
One real deterministic workflow plans, executes, verifies and reviews through the persistent Agent Run. Plan/steps/review are visible in Run status, normal review rejection is distinct from orchestration/runtime failure, and all existing Runtime/writer/verifier safety boundaries remain authoritative.
</success_criteria>
