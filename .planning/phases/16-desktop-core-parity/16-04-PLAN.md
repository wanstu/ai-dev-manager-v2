# 16-04 — Windows CI path repair and Desktop dogfood polish

User-authorized scope (2026-09-10): finish the existing Windows short/long path repair, MCP/Skill interaction polish, tray and opt-in Windows login startup. This explicit request brings tray/autostart forward from Phase 17; installer/updater/signing and release publication stay deferred.

Requirement mapping: ADM-CORE-001/005/007/010/012 for canonical root comparisons; ADM-CORE-015 and ADM-DESKTOP-001 for human management polish. Desktop shell preferences use HKCU Run, independently of Gateway state, and are not another ADM state store.

Acceptance: real Windows 8.3 aliases compare equally, missing descendants remain contained and outside roots remain rejected; full tests, vet and diff check; a real Wails build; manual executable checks for close/hide/show/quit, second launch and opt-in startup. Compilation alone cannot validate tray interaction.

Known defects found on review: ignored build/windows/icon.ico breaks non-Git copy tests and fresh checkouts; Go %q is inappropriate Windows executable command quoting; unsupported platforms must not hide into a nonexistent tray. Correct these before final gates. Check Desktop and tray startup checkbox synchronization and surface registry errors.

Deliver as two local commits: Windows canonicalization, then Desktop polish/tray/autostart. Do not tag, push or publish. Record evidence and outstanding manual checks without advancing release status.
