---
phase: 03-external-mcp-runtime-completion
plan: 03-02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/management/service.go
  - internal/desktop/adapter.go
  - cmd/ai-dev-manager/main.go
  - cmd/ai-dev-manager/main_test.go
autonomous: true
requirements: [MCP-RUN-01, MCP-RUN-02, MCP-RUN-03, MCP-RUN-04, MCP-RUN-05]

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-05: CLI mcp status --id ID --environment-id ENV_ID returns JSON health status"
    - "D-05: management.MCPHealth delegates to app.ProbeMCPHealth"
    - "D-05: desktop.ProbeMCPHealth delegates to management.MCPHealth"
    - "D-03: connectExternalMCP sets resolved headers via HTTPClient on StreamableClientTransport"
  artifacts:
    - internal/management/service.go — MCPHealth method
    - internal/desktop/adapter.go — ProbeMCPHealth method
    - cmd/ai-dev-manager/main.go — mcp status CLI command
    - cmd/ai-dev-manager/main_test.go — test for mcp status help output
  key_links:
    - "CLI mcp status → management.MCPHealth → app.ProbeMCPHealth"
    - "desktop.ProbeMCPHealth → management.MCPHealth → app.ProbeMCPHealth"
---

<objective>
Enrich the management surface with MCP health status: CLI `mcp status` command, management service delegate, and Desktop adapter passthrough. Then run the full validation suite to confirm all MCP-RUN-01 through MCP-RUN-05 requirements are satisfied.

Purpose: Make health status accessible to humans (CLI/Desktop) after Plan 03-01 proved the core lifecycle.
Output: CLI mcp status command, management/desktop adapter methods, full green validation suite.
</objective>

<execution_context>
@C:/Users/wanstu/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/wanstu/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-external-mcp-runtime-completion/03-CONTEXT.md
@.planning/phases/03-external-mcp-runtime-completion/03-RESEARCH.md
@.planning/phases/03-external-mcp-runtime-completion/03-PATTERNS.md
@.planning/phases/03-external-mcp-runtime-completion/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 03-02-01: Management + Desktop + CLI Passthrough</name>
  <files>internal/management/service.go, internal/desktop/adapter.go, cmd/ai-dev-manager/main.go, cmd/ai-dev-manager/main_test.go</files>

  <read_first>
    - internal/app/mcp_health.go (ProbeMCPHealth, MCPHealthStatus from Plan 03-01)
    - internal/management/service.go (current MCP methods at line 100-110)
    - internal/desktop/adapter.go (current MCP methods at line 162-181)
    - cmd/ai-dev-manager/main.go (runCatalog at line 505-612, runEnvironmentSelection at line 353-389)
    - cmd/ai-dev-manager/main_test.go (existing CLI test patterns)
  </read_first>

  <acceptance_criteria>
    - `management/service.go` gains `MCPHealth(ctx, environmentID, mcpID) (app.MCPHealthStatus, error)` method that delegates to `app.ProbeMCPHealth`.
    - `desktop/adapter.go` gains `ProbeMCPHealth(environmentID, mcpID) (app.MCPHealthStatus, error)` method that delegates to `management.MCPHealth`.
    - CLI `mcp status --id MCP_ID --environment-id ENV_ID` command added, prints JSON health status.
    - CLI `mcp status -h` prints usage help.
    - `go test ./internal/management/...` passes (or existing tests continue passing).
    - `go test ./cmd/ai-dev-manager/...` passes.
  </acceptance_criteria>

  <verify>
    <automated>go test ./internal/management/... ./cmd/ai-dev-manager/...</automated>
  </verify>

  <action>
Add `MCPHealth` method to `internal/management/service.go` that delegates to `app.ProbeMCPHealth` (per D-05 — existing management surface, no new surface needed). Add `ProbeMCPHealth` method to `internal/desktop/adapter.go` that delegates to `management.MCPHealth` (per D-05 — Desktop pass-through only, no feature expansion). Add `"status"` case to `runCatalog` in `cmd/ai-dev-manager/main.go` with `--id` and `--environment-id` flags, calling `service.ProbeMCPHealth` and printing JSON output. Add CLI test for `mcp status --help` in `cmd/ai-dev-manager/main_test.go`.
  </action>

  <artifacts>
    - internal/management/service.go — MCPHealth method
    - internal/desktop/adapter.go — ProbeMCPHealth method
    - cmd/ai-dev-manager/main.go — mcp status CLI command
    - cmd/ai-dev-manager/main_test.go — test for mcp status help output
  </artifacts>

  <done>
    CLI `mcp status` returns JSON health status. Management and Desktop adapters delegate to app.ProbeMCPHealth. No new management surface introduced.
  </done>
</task>

<task type="auto">
  <name>Task 03-02-02: Final Validation Suite</name>
  <files></files>

  <read_first>
    - All files modified in Plan 03-01 and Plan 03-02
    - internal/gateway/mcp_acceptance_test.go (full lifecycle test from Plan 03-01)
    - internal/app/mcp_health_test.go (health probe unit tests from Plan 03-01)
    - internal/gateway/server_test.go (existing tests that must remain green)
  </read_first>

  <acceptance_criteria>
    - `go test ./...` passes with zero failures.
    - `go vet ./...` passes.
    - `git diff --check` is clean.
    - All MCP-RUN-01 through MCP-RUN-05 acceptance tests pass.
    - Structured errors return `error_kind` and `mcp_id` without secret leakage.
    - Health states correctly distinguish configured/disabled/healthy/error.
    - At least one real external MCP call succeeds end-to-end through the Gateway.
    - No resolved secret values appear in any test output, error message, or inspection result.
    - Empty endpoint returns "configured" state (not "error").
    - Empty tool name in mcp_call returns structured error with `error_kind: "missing_tool_name"`.
    - Disabled Environment MCP access returns structured error with `error_kind: "not_enabled"`.
    - Broken MCP endpoint returns structured error with `error_kind: "connection_refused"` or `"timeout"`.
  </acceptance_criteria>

  <verify>
    <automated>go test ./... && go vet ./... && git diff --check</automated>
  </verify>

  <action>
Run full validation suite: `go test ./...`, `go vet ./...`, `git diff --check`. Verify all acceptance criteria are met. If any test fails, fix the underlying issue. This task is primarily a validation gate — no new code unless a pre-existing test breaks.
  </action>

  <done>
    Full suite green. All MCP-RUN-01 through MCP-RUN-05 requirements verified. No secret leakage in any output. Phase 3 implementation is complete.
  </done>
</task>

</tasks>

<threat_model>
Shared threat model is in Plan 03-01 (03-01-PLAN.md). No additional threats introduced by management surface enrichment — these are thin delegates over already-validated app layer.
</threat_model>

<verification>
1. `go test ./...` — full suite green
2. `go vet ./...` — no warnings
3. `git diff --check` — clean
4. All MCP-RUN-01 through MCP-RUN-05 acceptance tests pass
5. Real Streamable HTTP acceptance proves end-to-end health lifecycle
6. CLI `mcp status` returns structured health JSON
</verification>

<success_criteria>
- CLI mcp status returns JSON health status for any MCP in any Environment
- Management and Desktop adapters delegate to app.ProbeMCPHealth
- Full validation suite passes with zero failures
- No Phase 5 lifecycle leakage (LIFE-01..03 deferred)
- No Desktop feature expansion beyond minimal pass-through
</success_criteria>

<output>
Create `.planning/phases/03-external-mcp-runtime-completion/03-02-SUMMARY.md` when done
</output>
