# 16-05 — Saved ADM connections and modal editors

User request 2026-09-10: save/edit/switch multiple ADM connections in ~/.config/adm and replace jumping inline child editors with dialogs.

Scope: Desktop connection profiles (stable ID, name, base URL, active ID) in ~/.config/adm/desktop-connections.json; atomic writes, errors preserve the existing file, no secret-bearing URLs. This is Desktop connection preference storage, not a duplicate of ADM service state. Existing Admin MCP connectivity and loopback/bootstrap restrictions remain unchanged.

ADM-DESKTOP-001 / ADM-CORE-015: native modal add/edit MCP, import, Skill source, Workspace, Environment, executable and Memory forms, plus rename dialog. Preserve draft on failure; Escape/cancel closes and restores focus without scrolling the page. Successful save closes. Profile changes clear old management and observed health state and wait for in-flight work before switching.

Desktop icon polish requested 2026-09-10: use the three ADM icon sources under `assets/icons`. `ai-dev-manager-app.png` is the canonical Wails application/Windows executable icon source, `ai-dev-manager-tray.png` is converted to the embedded Windows tray `.ico`, and `ai-dev-manager-window.png` is used as the Desktop UI brand mark. Keep generated Wails build assets under the existing ignored `cmd/ai-dev-manager-desktop/build/` tree; the build script must refresh them deterministically before `wails build`. The local `ime-lock-v2` reference root is still outside this session's connector allow-roots, so implementation follows the same Wails asset split rather than claiming a line-by-line port.

Gates: profile persistence/validation/delete/active selection tests; UI behavior checks; icon source/build preparation checks; full go test, vet, diff check and Wails build. Local commit only; no tag/push/release. Prior tray interactive acceptance remains outstanding.
