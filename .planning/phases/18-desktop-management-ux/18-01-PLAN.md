---
phase: 18-desktop-management-ux
plan: "01"
type: execute
status: ready
depends_on: []
requirements:
  - ADM-DESKTOP-001
  - ADM-MGMT-001
  - ADM-CORE-003
  - ADM-CORE-004
  - ADM-CORE-012
  - ADM-CORE-013
  - ADM-CORE-015
  - ADM-CORE-017
  - ADM-CORE-020
---

# 18-01 — Desktop menu shell, dashboard and section routing

Planning date: 2026-09-11
Code baseline: `0173259`; scope review checkpoint: `d95b088`
Execution state: NOT STARTED. This file is a ready execution plan, not implementation evidence.

## Objective

Turn the current long Desktop panel into a grouped-menu application: a persistent connection/Environment context, a truthful read-only dashboard and ten routes to existing management sections. Preserve all current management actions through the existing Admin MCP adapter.

This plan is small by behavior: reorganize and reconnect existing UI, plus the minimum data-state/error isolation required for a truthful overview. It does not redesign feature managers or implement later Core features.

## Required context / preflight

1. Check `git status --short --branch` and `git log -3 --oneline`; confirm the expected repository/root, master and the recorded planning commits. Preserve unrelated changes.
2. Read `AGENTS.md`, `.planning/STATE.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/post-1.0/PHASE-MAP.md`, `18-CONTEXT.md`, `18-UI-REFACTOR.md` and this plan.
3. Refer to `docs/PRODUCT_CONTRACT.md` for the requirement IDs and to completed Phase 16 convergence/profile/modal records for current production behavior. Initial-slice wording does not authorize a writable offline fallback.
4. Before any write, acquire the writer lease for Environment `env_43a2d0ca74fbc0f1` rooted at `D:\projects\ai-dev-manager-v2`. Use a unique session owner, heartbeat during idle review, and release normally when done. Do not force-release someone else's active writer.
5. Read actual source before each bounded change. This planning session found only the root tracked AGENTS.md; check for newly introduced nested instructions before execution.
6. No subagents, push, tag, release, Core API changes or new product prerequisites.

## Scope / file boundary

Primary existing files (relative to repository root):

- `cmd/ai-dev-manager-desktop/frontend/index.html`
- `cmd/ai-dev-manager-desktop/frontend/styles.css`
- `cmd/ai-dev-manager-desktop/frontend/app.js`
- `cmd/ai-dev-manager-desktop/frontend/connections.js` and `dialogs.js`, only for shell lock/focus/clearing integration
- `cmd/ai-dev-manager-desktop/main_test.go`, only asset/binding assertions
- `.planning/phases/18-desktop-management-ux/`, STATE and ROADMAP for evidence/continuation

New narrow helpers:

- `cmd/ai-dev-manager-desktop/frontend/navigation.js`
- `cmd/ai-dev-manager-desktop/frontend/dashboard.js`
- `tests/desktop-ui/navigation.test.cjs`, `dashboard.test.cjs` and a small real-browser fixture/stub if needed. Test-only files stay outside embedded frontend assets.

No changes planned to Go Core/model/store/Gateway/Runtime, Desktop adapter API, production command/tray lifecycle or build/dependency configuration. If a concrete contradiction requires one, save it in STATE and revise this plan before expanding scope.

## Task 1 — Navigable shell and preserved feature sections

- [ ] Introduce the compact connection/context header and grouped sidebar from the design.
- [ ] Move each existing management panel to its section once; preserve existing control IDs, form associations, action data attributes and listener ownership.
- [ ] Implement fixed hash routes: overview, workspaces, environments, runtime, mcp, skills, memory, gateway, exec-allowlist, settings. Default/unknown routes resolve to overview; menu, overview links and back/forward use one handler.
- [ ] Preserve the single connectionSelect and managementEnvironment controls across all routes. Move connection details/actions to gateway, launchAtLogin to settings and snapshot metrics to overview.
- [ ] Memory retains explicit Global Memory load and links to the current Environment's existing detail/Memory controls. Diagnostics shortcut routes to Environment inspection; no empty placeholder diagnostics page.
- [ ] Keep native editors and Environment detail/backdrop outside hidden sections; ensure the whole actionable shell is covered by the existing connection transition lock.
- [ ] Provide exactly one active page/menu state, keyboard navigation, visible focus and section-heading focus. Prevent route/hash changes from hiding an open editor/detail or discarding its draft.
- [ ] Keep filters and selected Environment stable across section changes. Do not change the established Environment selection behavior merely by entering a route.
- [ ] Add scoped hidden-page and responsive shell rules; confirm layout at 1120x760 and 820x560.

Acceptance: validation A01/A02/A06. Every route has real existing functionality; navigation adds no adapter call or mutation. No duplicated control IDs or lost dialogs.

Commit node: `feat(desktop): add management section navigation`.
Before committing, record changed files, checks and next task in planning; incomplete work must remain explicitly marked.

## Task 2 — Truthful dashboard and scoped read status

- [ ] Dashboard renders only supplied sanitized snapshot/status facts. Reuse six metric IDs and link to their relevant section.
- [ ] Keep transient state distinct for not loaded, loading, success/empty, error and optionally stale last-success. Do not use synthetic empty snapshot data as evidence of zero resources.
- [ ] Render successful GetSnapshot data independently from ListSkillSources / selected Environment / Skill availability / Runtime follow-up reads. An auxiliary failure keeps valid base inventory visible and reports a local error/retry.
- [ ] Maintain existing explicit connection/refresh/current-Environment read behavior; do not introduce a new polling loop, cross-Environment scan or page-entry fan-out.
- [ ] Ensure profile switch/disconnect clears prior target state and never falls back to local persistence. Keep the existing serialized bridge proxy and in-flight transition handling.
- [ ] Key asynchronous view commits to the current profile/connection generation and, where relevant, Environment ID/request generation. Discard old-scope results; do not replay/retarget mutations.
- [ ] Do not render a prior Environment's Memory/output/detail as the current selection. Read-only summary shows counts only; explicit Memory reads remain behind the existing Load operations.
- [ ] Display unavailable optional capability reasons locally. A missing Git/verifier/Skill/MCP does not disable shell or unrelated management.
- [ ] Add narrow executable tests for route normalization, state transitions, valid-zero vs unavailable, partial auxiliary failure, and stale response rejection using controlled bridge results.

Acceptance: validation A03/A04/A05/A07/A08. Configured inventory is never called healthy Runtime. Routing/dashboard does not call Memory values, MCP probes, source refresh, writer acquire, verifier run, or process/run mutation.

Commit node: `feat(desktop): add management overview and scoped loading states`.

## Task 3 — Interaction verification and plan closeout

- [ ] Retain useful embedded asset/binding checks and update them only to reflect actual asset movement. Do not replace behavioral validation with additional string markers.
- [ ] Exercise the production HTML/JS with a fake Wails bridge in a real browser: all ten routes, shortcut/back/forward behavior, hidden controls, profile switch, Memory explicit load and pending-read cases. No source copies or hand-built DOM substitutes as proof of browser behavior.
- [ ] Check failed-save draft preservation, modal Escape/cancel/focus return, long paths/lists and scaling. Run the existing Wails build path and test the resulting artifact in a real Desktop window.
- [ ] Run the gates in 18-VALIDATION.md, keeping long commands in asynchronous ADM run_start/status rather than repeating blocking exec after an outer timeout.
- [ ] Write `18-01-SUMMARY.md` with actual changes, commit hashes, test/artifact evidence, manual items, remaining risks and next recommended slice. Update STATE/ROADMAP and this plan status.
- [ ] Mark 18-01 complete only when required behavior and Wails smoke evidence are present. If GUI access is unavailable, retain an explicit implementation-complete / manual-acceptance-pending checkpoint; do not report full completion.
- [ ] Commit the validation/docs node, verify status, release the writer and stop. Do not automatically start 18-02 or publish.

Suggested final node: `docs: record Phase 18-01 validation and handoff`.

## Verification commands

Run from the repository root using existing allowed development tools:

- Focused Go gate: `go test -count=1 ./cmd/ai-dev-manager-desktop ./internal/desktop ./internal/management ./internal/adminmcp`.
- New narrow JS gate after those tests exist: `node --test tests/desktop-ui/navigation.test.cjs tests/desktop-ui/dashboard.test.cjs`. Node is test tooling only; the shipped Desktop still requires no frontend build pipeline.
- Integration gate once at the final code node: `go test -count=1 ./...` and `go vet ./...`.
- Wails artifact: `powershell -NoProfile -File scripts/build-desktop.ps1 -OutputName adm-desktop-phase18-01-windows-amd64.exe`. Use a distinct artifact name; do not replace a running Gateway/Desktop binary.
- `git diff --check`, and after explicit staging, `git diff --cached --check`.
- Real-browser and Wails manual checks: 18-VALIDATION.md. A successful raw Go build is not a replacement for Wails build/launch.

These commands are planned execution gates. No code tests/build/UI acceptance were run by the planning-only session.

## Interruption and quota continuity

At each task/commit boundary, persist the exact last completed action, current branch/head, touched files, remaining checks and next command in STATE and the plan/summary. Include any running ADM run IDs and whether the writer was released. Never put secret values or Memory contents in planning evidence.

If interruption happens before a clean node: save the partial status before doing more work, avoid claiming completion, and release the writer when possible. The next session checks Git and writer ownership before continuing. If a command/commit response is lost, inspect run/status or git log/status before retrying; never blindly repeat a mutation.

Initial resume action after this planning session: acquire a fresh session lease after Git/context checks, then start Task 1. Task 1/2/3 are all pending.
