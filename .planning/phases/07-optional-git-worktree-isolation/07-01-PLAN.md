---
phase: 07-optional-git-worktree-isolation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PRODUCT_CONTRACT.md
  - internal/model/types.go
  - internal/environment/service.go
  - internal/environment/service_test.go
  - internal/isolation/service.go
  - internal/isolation/service_test.go
  - internal/app/service.go
  - internal/app/service_test.go
  - internal/gateway/server.go
  - internal/gateway/server_test.go
  - .planning/phases/07-optional-git-worktree-isolation/
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [ISO-01, ISO-02, ISO-03, ADM-CORE-002, ADM-CORE-003, ADM-CORE-009, ADM-CORE-010, ADM-DEV-001, ADM-DEV-002, ADM-DEV-003]
must_haves:
  truths:
    - Git worktree isolation is optional and ordinary non-Git Environment development remains unchanged.
    - Managed worktree roots are created only under an ADM-owned state-directory root and source checkout state is not switched or rewritten.
    - Two managed worktree Environments from one Git Workspace have distinct roots and branches.
    - Managed roots are revalidated before routed Runtime access so missing or tampered worktrees cannot receive mutations.
    - Destroy is writer-exclusive, refuses dirty/unpublished work by default, requires explicit force for unsafe removal, and never deletes the managed branch.
  artifacts:
    - path: internal/isolation/service.go
      provides: managed Git worktree create/list/validate/destroy lifecycle
    - path: internal/app/service.go
      provides: Runtime revalidation seam and app-facing isolation operations
    - path: .planning/phases/07-optional-git-worktree-isolation/07-VERIFICATION.md
      provides: real local Git acceptance evidence
  key_links:
    - from: environment_worktree_create Gateway tool
      to: isolation.Service
      via: Workspace Git capability and ADM-owned destination generation
    - from: app.Service.Runtime
      to: isolation.Service.ValidateEnvironment
      via: operation-local managed-root revalidation
    - from: environment_worktree_destroy Gateway tool
      to: isolation.Service
      via: writer ownership plus dirty/unpublished safety policy
---

<objective>
Add safe optional managed Git-worktree isolation around Environment without making Git intrinsic to Workspace/Environment or crossing into Agent Run/parallel orchestration phases.
</objective>

<context>
@AGENTS.md
@docs/PRODUCT_CONTRACT.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-optional-git-worktree-isolation/07-CONTEXT.md
@.planning/phases/07-optional-git-worktree-isolation/07-RESEARCH.md
@.planning/phases/07-optional-git-worktree-isolation/07-VALIDATION.md
@.planning/phases/06-dev-process-logs-ports/06-INTEGRATION-REVIEW.md
</context>

<tasks>
<task type="auto">
<name>1. Add separate managed-worktree state and safe Git lifecycle</name>
<files>docs/PRODUCT_CONTRACT.md; internal/model/types.go; internal/environment/service.go; internal/environment/service_test.go; internal/isolation/service.go; internal/isolation/service_test.go</files>
<action>Add a separate persisted managed-worktree record, internal managed-root Environment creation/removal seams, ADM-owned destination generation, source-repository validation, generated branch creation, identity revalidation and writer-exclusive safe destroy. Generic Environment creation remains Workspace-contained and generic Environment removal refuses managed roots.</action>
<verify>P1-P7 service-level tests use real temporary Git repositories/worktrees; no arbitrary destination/branch input exists; branches are retained after destroy.</verify>
<done>One Git Workspace can safely create/destroy isolated Environment roots while non-Git Environment semantics remain unchanged.</done>
</task>

<task type="auto">
<name>2. Route managed-root validation and Agent Gateway tools</name>
<files>internal/app/service.go; internal/app/service_test.go; internal/gateway/server.go; internal/gateway/server_test.go</files>
<action>Wire isolation service into app construction, revalidate any managed/out-of-Workspace Environment before Runtime construction, expose environment_worktree_create/list/destroy Gateway tools, and preserve operation-local failure semantics. Ensure Gateway-owned dev-process startup inherits the same Runtime validation seam.</action>
<verify>P4/P8 prove tamper rejection before mutation, generic environment_remove refusal, Agent tool routing, and ordinary non-Git tools remain green.</verify>
<done>Agents can request managed worktree isolation through ADM identity without gaining arbitrary Git/filesystem authority.</done>
</task>

<task type="auto">
<name>3. Real local Git acceptance and Phase 7 closure evidence</name>
<files>.planning/phases/07-optional-git-worktree-isolation/07-01-SUMMARY.md; 07-VERIFICATION.md; 07-UAT.md; .planning/STATE.md; .planning/PROJECT.md; .planning/ROADMAP.md</files>
<action>Run deterministic current-source acceptance against real temporary local Git repositories on Windows: two managed worktrees, unchanged source checkout, cross-root mutation isolation, missing/tampered rejection, safe destroy and force/branch-retention cases. Run focused/full Go regression, vet and diff-check. Record evidence only; do not merge master or push automatically.</action>
<verify>P1-P8 plus `go test ./...`, `go vet ./...`, `git diff --check`.</verify>
<done>Phase 7 is locally verified and stops for integration review; Phase 8 is not started automatically.</done>
</task>
</tasks>

<success_criteria>
ISO-01..03 pass with real Git worktree filesystem behavior. Git/worktree remains an optional capability, managed roots are ADM-owned and revalidated, unsafe destroy requires explicit force with branch retention, source checkout is unchanged, and no Phase 8/11 orchestration scope is introduced.
</success_criteria>
