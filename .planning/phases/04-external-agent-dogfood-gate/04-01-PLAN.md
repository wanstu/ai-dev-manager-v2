---
phase: 04-external-agent-dogfood-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - internal/runtime/process_windows.go
  - internal/runtime/cancellation_windows_test.go
  - .planning/phases/04-external-agent-dogfood-gate/
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [ADM-GOAL-001, ADM-DEV-004, SKILL-RUN-05, VERIFY-03, ADM-CORE-013]
must_haves:
  truths:
    - External Agent completes a real code change through ADM only.
    - GSD Skill is consumed through Environment-gated ADM reads.
    - Structured verifier drives a failing-test and follow-up passing-test cycle.
    - Memory and configuration survive an owned Gateway restart.
    - R1 stops for review before Phase 5.
  artifacts:
    - path: .planning/phases/04-external-agent-dogfood-gate/04-VERIFICATION.md
      provides: observed acceptance evidence
  key_links:
    - from: adm-verifier-report
      to: environment_verifier_run
      via: actual result JSON consumed by CLI
---

<objective>
Complete one real ADM-only development loop by building a standalone verifier-result reporting CLI in D:/projects/adm-verifier-report. No ADM runtime feature changes are planned.
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-external-agent-dogfood-gate/04-CONTEXT.md
@.planning/phases/04-external-agent-dogfood-gate/04-RESEARCH.md
@.planning/phases/04-external-agent-dogfood-gate/04-VALIDATION.md
</context>

<tasks>
<task type="auto">
<name>1. Establish current R1 Gateway and target context</name>
<files>.tmp/phase4-runtime; D:/projects/adm-verifier-report</files>
<action>Build CLI from current branch through ADM. Start private Gateway with explicit ADM_V2_HOME under ignored .tmp and a free loopback port. Do not stop shared 41137. Register target directory via ADM; create it only if absent through an existing registered root/writer. Inspect any existing target before mutation. Discover explicit installed GSD root/support root, enable gsd-next only for target; read Skill/workflow and run deterministic smart-entry for Phase 4. Capture disabled read rejection before enabling. Declare go test and go vet verifiers through existing ADM CLI, allow only required executables. Read/write private task Memory.</action>
<verify>D1,D2,D5; identity and tools/list; no .git and no git.* capability; file read works with zero verifiers.</verify>
<done>Task-owned current Gateway is available; target has scoped context and real Skill consumption.</done>
</task>
<task type="auto">
<name>2. Implement and consume verifier report CLI</name>
<files>D:/projects/adm-verifier-report/go.mod; main.go; main_test.go; README.md</files>
<action>Create a stdlib-only Go CLI reading exactly one result envelope from stdin. Print verifier identity/status without dumping stdout/stderr. Exit 0 only when required fields exist, status passed, exit_code zero and timed_out false; valid non-pass exits 1; invalid/incomplete/contradictory evidence exits 2. Add boundary tests, invoke structured verifier and inspect actual failure, make follow-up exact edit, rerun to pass. Save actual failed/passed result envelopes and feed them to the built CLI through ADM. Keep source snapshots with Phase evidence so the external non-Git change is reviewable.</action>
<verify>D3,D4; red/green structured results, actual CLI exit codes, go vet verifier.</verify>
<done>Real source correction and evidence consumption complete; no direct filesystem/exec fallback.</done>
</task>
<task type="auto">
<name>3. Restart, regress and record R1 gate</name>
<files>.planning/phases/04-external-agent-dogfood-gate/04-01-SUMMARY.md; 04-VERIFICATION.md; 04-UAT.md; .planning/STATE.md; .planning/PROJECT.md; .planning/ROADMAP.md</files>
<action>Use existing ADM lifecycle to stop/start only the private Gateway. Reconnect and assert stable Environment/Skill/verifier/private Memory identity and project reads; assert a second Environment cannot read target-private Memory. Release writers and stop owned Gateway after evidence capture. Run repository tests/vet/diff check through ADM; record known flake if observed. Run only deterministic GSD consistency/completeness/UAT scripts; if a gate strictly demands an LLM agent stop at that gate and record it. Update Phase truth only after evidence passes. Stop at R1 for review, no Phase 5 implementation.</action>
<verify>D5,D6,D7,D8 plus go test ./..., go vet ./..., git diff --check.</verify>
<done>Reviewable evidence and truthful Phase status persist; owned runtime cleaned up.</done>
</task>
</tasks>

<success_criteria>
D1-D6 and D8 pass; D7 explicitly N/A. All project mutations use ADM writer or existing ADM CLI application boundary. No new product prerequisites or LLM agents. External target source/evidence are preserved for review.
</success_criteria>

## Blocker amendment — 2026-09-07
Two independent runs (full suite and focused -count=1) failed TempDir cleanup of verifier_timeout_fixture because Windows retained a running process. Runtime.Exec uses exec.CommandContext and Windows configureCommand only hides the console; cancellation has no process-tree handling. Add bounded Windows cancellation for the owned command tree using Windows' built-in taskkill /T /F, with direct-process kill fallback if cleanup fails; no change to timeout classification, no global Environment prerequisite and no new installed dependency. This is short-lived command cancellation only, not Phase 5 persistent lifecycle. Add a Windows parent/child cancellation regression, retain the real HTTP acceptance unchanged, then rerun it and the full suite. No retries/sleeps are added to acceptance cleanup to mask leaked processes.
User additionally authorizes @pj only to repair @pjadm when unusable; return to @pjadm afterward. @pjadm remained usable here, so no fallback was needed.
