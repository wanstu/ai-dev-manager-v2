---
phase: 08-agent-run-lifecycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PRODUCT_CONTRACT.md
  - internal/gateway/runtime_owner.go
  - internal/gateway/runtime_run.go
  - internal/gateway/runtime_run_test.go
  - internal/gateway/run_acceptance_test.go
  - internal/gateway/server.go
  - internal/gateway/server_test.go
  - .planning/phases/08-agent-run-lifecycle/
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [ARUN-01, LIFE-01, LIFE-03, ADM-CORE-005, ADM-CORE-007, ADM-GW-001, ADM-GW-003, ADM-DEV-001, ADM-DEV-003]
must_haves:
  truths:
    - A stable `run_` resource is owned by the persistent Gateway rather than by the launching Agent client.
    - A later client can list/status/cancel the same running Run by stable ADM identity.
    - Run command execution reuses existing Runtime allowlist/cwd/managed-root validation and writer authority.
    - Terminal lifecycle distinguishes succeeded, failed and canceled without inventing Phase 9 workflow semantics.
    - Gateway shutdown cancels running Runs; restart starts with no prior Run observations and state.json contains no Run observations.
  artifacts:
    - path: internal/gateway/runtime_run.go
      provides: owner-local Agent Run lifecycle and cancellation
    - path: internal/gateway/server.go
      provides: Agent Gateway run_start/list/status/cancel tools
    - path: .planning/phases/08-agent-run-lifecycle/08-VERIFICATION.md
      provides: real HTTP owner/restart acceptance evidence
  key_links:
    - from: run_start
      to: app.Service.Runtime + runtime.Runtime.Exec
      via: writer-gated asynchronous owner context
    - from: run_cancel
      to: owned Run context
      via: matching writer and stable run identity
    - from: runtimeOwner.Close/DropEnvironment
      to: Run cleanup
      via: bounded cancellation and no persistence
---

<objective>
Add the first real persistent-owner Agent Run lifecycle as a single asynchronous Runtime command, proving stable identity/status/cancel semantics without implementing Planner/Executor/Reviewer orchestration.
</objective>

<context>
@AGENTS.md
@docs/PRODUCT_CONTRACT.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-agent-run-lifecycle/08-CONTEXT.md
@.planning/phases/08-agent-run-lifecycle/08-RESEARCH.md
@.planning/phases/08-agent-run-lifecycle/08-VALIDATION.md
@internal/gateway/runtime_owner.go
@internal/gateway/runtime_process.go
@internal/gateway/process_acceptance_test.go
</context>

<tasks>
<task type="auto">
<name>1. Add owner-local Run lifecycle over existing Runtime authority</name>
<files>docs/PRODUCT_CONTRACT.md; internal/gateway/runtime_owner.go; internal/gateway/runtime_run.go; internal/gateway/runtime_run_test.go</files>
<action>Define the ARUN-01 product contract and implement owner-local `run_` resources with running/succeeded/failed/canceled states. Start requires the matching Environment writer, resolves the existing app Runtime, validates execution through Runtime.Exec, launches asynchronously under an owner-derived context, heartbeats the writer while running, retains bounded command result/status in owner memory, and supports writer-gated cancel. Owner close and Environment drop cancel Runs; no Run fields are persisted.</action>
<verify>P1/P3-P7 focused tests prove lifecycle transitions, wrong-writer/authority failures, owner/Environment cleanup, bounded cancellation, and absence of persisted Run state.</verify>
<done>Run lifecycle is a real persistent-owner runtime capability, not metadata CRUD and not a second execution policy.</done>
</task>

<task type="auto">
<name>2. Expose stable Run lifecycle through the Agent Gateway</name>
<files>internal/gateway/server.go; internal/gateway/server_test.go; internal/gateway/run_acceptance_test.go</files>
<action>Add `run_start`, `run_list`, `run_status`, and `run_cancel` tools. Prove through real Streamable HTTP that start returns while a helper remains running, the launching client can disconnect, a later client sees/cancels the same Run, owner shutdown cleans active Runs, and restart returns an empty Run set with old IDs invalid.</action>
<verify>P1-P8 including real HTTP restart acceptance and unrelated process/file Gateway regression.</verify>
<done>External Agents can consume ARUN-01 by stable ADM identity across client invocations while one Gateway owner remains alive.</done>
</task>

<task type="auto">
<name>3. Full regression and Phase 8 local verification evidence</name>
<files>.planning/phases/08-agent-run-lifecycle/08-01-SUMMARY.md; 08-VERIFICATION.md; 08-UAT.md; evidence/regression.json; .planning/STATE.md; .planning/PROJECT.md; .planning/ROADMAP.md</files>
<action>Run deterministic focused and real HTTP acceptance, full Go regression, vet and diff-check. Record evidence and move ARUN-01 to locally verified only after all gates pass. Do not merge master, push, start Phase 9, or invoke LLM subagents.</action>
<verify>`go test ./...`; `go vet ./...`; `git diff --check`; focused Run lifecycle/restart acceptance.</verify>
<done>Phase 8 stops locally verified for integration review with Phase 9 not started.</done>
</task>
</tasks>

<success_criteria>
ARUN-01 passes with real Gateway-owner behavior: stable Run identity, later-client status/cancel, correct terminal states, existing Runtime/writer authority, owner cleanup, and no restart resurrection/persistence. No Phase 9-11 orchestration scope is introduced.
</success_criteria>
